/**
 * Artist Collection Route
 *
 * Handles /collections/:artist URLs for individual artist collections.
 * Re-exports the main collections component with param support.
 */

// Re-export everything from the main collections file
export { loader, meta, default } from "./collections";
